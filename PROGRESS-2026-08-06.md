# Progress — Kamis, 6 Agustus 2026

> Catatan harian kemajuan development PGS (SIM Sekolah).
> 26 commit hari ini (c5f13e2 → 7f090c9). **Belum di-push: `7f090c9`** (sisanya sudah di `origin/main`).

## Ringkasan per Area

### 1. Absensi — UX Presensi Manual (`8c776f1`, `597b9e4`, `6cc0a5c`)
- **Segmented control** status 5-in-1 compact (`src/components/absensi/StatusSegmented.tsx`): Hadir=emerald, Terlambat=amber, Izin=blue, Sakit=purple, Alpha=red — dipakai di 4 render site (siswa/guru × desktop/mobile)
- **Autosave per baris** (debounce 700ms, optimistic update, indikator menyimpan/tersimpan/gagal di kolom nama) — data langsung terbaca di Rekap
- **Auto-fill jam datang** dari Pengaturan Presensi (`jamMasukSiswa`/`jamMasuk`); jam & keterangan kondisional (tersembunyi utk status yang tidak relevan; keterangan utk Izin/Sakit/Alpha/Terlambat)
- **Sticky toolbar**: filter + search menyatu + bulk action (Hadir Semua primary) + stats + indikator autosave
- Tabel desktop memakai **neumo-card** (konsisten dgn modul lain)
- Emergency mode "pulang cepat" (`6cc0a5c` — oleh user)

### 2. Akademik — Generate Mapel Kurikulum + Import/Export (`3220818`)
- **`src/data/kurikulumKMA1503.ts`**: standar KMA 1503/2025 (MI/MTs/MA) + Permendikdasmen 13/2025 (SD/SMP/SMA/SMK/TK), 8 jenjang, ~13-19 mapel per jenjang dgn kode/JP/kelas/peminatan
- **GenerateKurikulumDialog** (neumorphism + framer-motion): filter kelas, preview (Baru/Sudah Ada), JP editable, simpan via `mapel.generateFromKMA` (upsert + reorder)
- **Import/Export XLSX mapel** (`ImportExportMapel` → `mapel.importBulk`)
- Kolom baru `jumlah_jam` di `mata_pelajaran` (migration `0019`)
- Catatan: data kurikulum **direkonstruksi** dari standar resmi (file ZITE asli tidak ada di disk) — angka JP perlu validasi ulang

### 3. Akademik — AI Auto-Generate Jadwal (`13adf69`, `433f14e`, `6a4a618`, `a2255b4`, `66afdf7`)
- **splitJP()**: maks 3 JP/pertemuan, min 2, pecahan merata (4→2+2, 5→3+2, 6→3+3, 7→3+2+2)
- **Validasi overload**: beban vs kapasitas per kelas → stop + pesan solusi (kurangi JP / tambah slot / kurangi hari libur); blok yang gagal ter-place = error, bukan silent drop
- **Distribusi merata**: `activeDays` urut deterministik + least-loaded balancing (hari paling kosong duluan) — hasil dry-run: 39/39 JP, per hari 7/7/7/10/8 (sebelumnya selasa/rabu 0-4 JP)
- **Validasi tenant**: alokasi (kelas/guru/mapel) wajib milik sekolah sendiri — cegah generate lintas sekolah
- **Reset Jadwal** (`clearAll` per sekolah/per kelas) + **Reset JP/Kosongkan Hari** (`clearTimelineDay` bulk — **58 dtk → 0,9 dtk**, 62× lebih cepat)
- maxSteps 2000 → 20000; grouping dibatasi 3 JP

### 4. Finance — Backlog A1, B1-B4, D1, E1, E2 (`834d7c3`, `bd64f08`, `f53e699`, `d2266e6`, `e4998a5`)
- **A1**: FK `invoice.student_id`/`academic_year_id` + `discount.student_id` + `fee_structure.academic_year_id` (migration `0015`); fix bug hardcode `academicYearId: "active"`
- **B1-B3**: `/api/cron/finance` (auto-overdue + denda, proses reminder, cleanup rate limit) + `/api/payment/webhook` (HMAC + rate limit + audit)
- **B4**: sistem `tagihan_spp` lama dihapus total (migration `0017`)
- **D1**: `syncUserCredentials()` — 8 path mutasi password terpusat; `users` = single source
- **E2**: rate limiter DB-backed (`rate_limit_bucket`, migration `0016`) — terpasang di login (5x/15menit), reset password, bulk import
- **E1**: 26 index performa hot-path (migration `0018`)
- Env vars baru: `CRON_SECRET`, `PAYMENT_WEBHOOK_SECRET` (+ opsional `WHATSAPP_API_URL`/`SMTP_HOST`)

### 5. Keamanan Tenant — C1 (`f35269f`)
- `requireSekolahId` wajib di ~62 mutation / 25 router; 7 lubang cross-tenant finance & asesmen ditutup; 3 fallback "sekolah pertama di DB" dihapus

### 6. Kaldik — Auto-suggest Tahun Ajaran (`6d0a713`, `b2c5d95`)
- `src/server/kaldik.ts` + tabel `pengaturan_kalender` (migration `0014`); auto-fill tanggal semester sesuai pola Kaldik nasional (align Senin)
- Perbaikan besar rantai migration: snapshot 0011-0013 yang hilang dibangun ulang, ghost `0005` dihapus, DB disinkronkan dgn `__drizzle_migrations`

## Verifikasi
- tsc ✓ / lint 0 error ✓ / build ✓ di tiap milestone
- Benchmark terukur: clearTimelineDay 58 dtk → 0,9 dtk; auto-generate 39/39 JP merata; overload & lintas-sekolah ditolak dengan pesan jelas
- Data dev: 2 tahun ajaran aktif dinonaktifkan 1 (duplikat), orphan fee_structure diperbaiki, jadwal di-reset saat pengujian & di-generate ulang

## Tindak Lanjut / Catatan
- ⏸️ RLS (C3): aktifkan sebelum produksi (`rls_implementation_pn.md`)
- 🔎 Data kurikulum KMA 1503 — validasi angka JP per mapel
- 🧹 Kelas duplikat/test (Kelas 8C 0 siswa, sekolah-1 demo) — dibiarkan sementara (keputusan user), audit lebih lanjut jika mengganggu
- 📌 Timeline senin sempat terhapus saat benchmark — sudah direkonstruksi (10 JP berurutan, pola sama dgn selasa); cek ulang di Pengaturan Jadwal bila berbeda
- 🚀 Belum di-push: `7f090c9` (instant tab switching + schedule loading skeleton)
