--- src/components/SiswaView.tsx
+++ src/components/SiswaView.tsx
@@ -5,2 +5,3 @@
 import React, { useState, useRef, useEffect } from 'react';
-import { Search, UserPlus, MoreVertical, Eye, Edit3, Key, Trash2, ShieldAlert, ChevronLeft, ChevronRight, FileUp, FileDown } from 'lucide-react';
+import { Search, UserPlus, MoreVertical, Eye, Edit3, Key, Trash2, ShieldAlert, ChevronLeft, ChevronRight, FileUp, FileDown, CheckCircle, AlertCircle, X } from 'lucide-react';
 import { Student, Institution, getPdfLogo } from '../types';
@@ -29,2 +30,8 @@
   const [activeImportType, setActiveImportType] = useState<'quick'|'regular'>('quick');
+
+  const [importPreviewData, setImportPreviewData] = useState<{
+    students: Student[];
+    errors: { rowIndex: number; rowName: string; messages: string[] }[];
+    type: 'quick'|'regular';
+  } | null>(null);
 
@@ -628,8 +635,9 @@
         if (activeImportType === 'quick') {
           const missingQuick: string[] = [];
           if (idxNama === -1) missingQuick.push('Nama Lengkap');
           if (idxNis === -1) missingQuick.push('NIS');
           if (idxPassword === -1) missingQuick.push('Password');
           if (idxJk === -1) missingQuick.push('Jenis Kelamin');
 
           if (missingQuick.length > 0) {
             alert(`Impor Cepat membutuhkan kolom berikut yang tidak ditemukan: ${missingQuick.join(', ')}`);
             return;
@@ -657,3 +665,4 @@
 
         // Parse rows
+        const errors: { rowIndex: number; rowName: string; messages: string[] }[] = [];
         for (let i = 1; i < rows.length; i++) {
           const columns = rows[i];
@@ -675,5 +684,5 @@
           // Basic check
           if (!nama || !nis || !jkRaw) {
             continue;
           }
 
@@ -689,6 +698,12 @@
           if (activeImportType === 'regular') {
             nik = getVal(idxNik).replace(/\s+/g, '');
             tempatLahir = getVal(idxTempatLahir);
             
+            // NIK Validation
+            if (nik && nik.length !== 16) {
+               rowErrors.push('NIK harus 16 digit');
+            }
+
             const rawDate = getVal(idxTanggalLahir);
             if (rawDate) {
               if (/^\d+$/.test(rawDate)) {
@@ -733,14 +748,9 @@
             namaIbu = getVal(idxNamaIbu);
 
             if (!nik || !tempatLahir || !tanggalLahir || !namaAyah || !namaIbu) {
-              alert(`Baris ke-${i + 1} (${nama}) memiliki data wajib reguler yang kosong. NIK, Tempat Lahir, Tanggal Lahir, Nama Ayah, dan Nama Ibu wajib diisi.`);
-              return;
-            }
-            if (nik.length !== 16) {
-              alert(`Baris ke-${i + 1} (${nama}) memiliki NIK yang tidak valid (harus 16 digit).`);
-              return;
+              rowErrors.push('Data wajib reguler (NIK, Tempat Lahir, Tanggal Lahir, Nama Ayah, Nama Ibu) belum lengkap');
             }
           }
 
           const studentId = `std-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
           const username = nis.toLowerCase(); // generated automatically from NIS as requested
@@ -749,2 +759,10 @@
           const nisn = getVal(idxNisn);
+
+          if (nis.length !== 6) {
+            rowErrors.push('NIS harus 6 angka');
+          }
+          if (nisn && nisn.length !== 10) {
+            rowErrors.push('NISN harus 10 angka');
+          }
+
           const hp = '';
@@ -803,11 +821,11 @@
           };
           parsedStudents.push(newStudent);
+          if (rowErrors.length > 0) {
+            errors.push({ rowIndex: i + 1, rowName: nama, messages: rowErrors });
+          }
         }
 
         if (parsedStudents.length === 0) {
           alert('Tidak ada data siswa valid yang ditemukan di file.');
           return;
         }
 
-        onImportStudents(parsedStudents);
-        alert(`Berhasil mengimpor ${parsedStudents.length} data siswa (${activeImportType === 'quick' ? 'Impor Cepat' : 'Impor Lengkap'}).`);
+        setImportPreviewData({ students: parsedStudents, errors, type: activeImportType });
       } catch (err) {
